version: '3.8'

services:
  dynamic_api:
    build:
      context: .
      args:
        GIT_NAME: Charl Cronje
        GIT_EMAIL: charlcp@gmail.com
    ports:
      - "6024:6024"
    volumes:
      - ./scripts:/app/scripts
